# Generated by generate_templates.py from rules-manifest.json. Do not edit by hand.
AWSTemplateFormatVersion: '2010-09-09'
Description: 'FedRAMP 20x SDR: provider-deployed Config custom rules (evidence-existence).
  A COMPLIANT result is telemetry, not a compliance determination.'
Parameters:
  EvidenceBucket:
    Type: String
    Description: S3 bucket (in your account) holding evidence artifacts.
  LambdaCodeS3Bucket:
    Type: String
    Description: S3 bucket holding the packaged Lambda zip.
  LambdaCodeS3Key:
    Type: String
    Description: S3 key of the packaged Lambda zip.
Resources:
  EvidenceRuleLogKey:
    Type: AWS::KMS::Key
    Properties:
      Description: CMK encrypting the evidence-existence Lambda log group.
      EnableKeyRotation: true
      KeyPolicy:
        Version: '2012-10-17'
        Statement:
        - Sid: AllowAccountAdmin
          Effect: Allow
          Principal:
            AWS:
              Fn::Sub: arn:aws:iam::${AWS::AccountId}:root
          Action: kms:*
          Resource: '*'
        - Sid: AllowCloudWatchLogs
          Effect: Allow
          Principal:
            Service:
              Fn::Sub: logs.${AWS::Region}.amazonaws.com
          Action:
          - kms:Encrypt
          - kms:Decrypt
          - kms:ReEncrypt*
          - kms:GenerateDataKey*
          - kms:Describe*
          Resource: '*'
  EvidenceRuleLogGroup:
    Type: AWS::Logs::LogGroup
    Properties:
      LogGroupName:
        Fn::Sub: /aws/lambda/${EvidenceRuleFunction}
      RetentionInDays: 365
      KmsKeyId:
        Fn::GetAtt:
        - EvidenceRuleLogKey
        - Arn
  EvidenceRuleRole:
    Type: AWS::IAM::Role
    Properties:
      AssumeRolePolicyDocument:
        Version: '2012-10-17'
        Statement:
        - Effect: Allow
          Principal:
            Service: lambda.amazonaws.com
          Action: sts:AssumeRole
      ManagedPolicyArns:
      - arn:aws:iam::aws:policy/service-role/AWSLambdaBasicExecutionRole
      Policies:
      - PolicyName: evidence-existence-readonly
        PolicyDocument:
          Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action:
            - s3:GetObject
            - s3:GetObjectTagging
            Resource:
              Fn::Sub: arn:aws:s3:::${EvidenceBucket}/*
          - Effect: Allow
            Action: config:PutEvaluations
            Resource: '*'
          - Effect: Allow
            Action:
            - xray:PutTraceSegments
            - xray:PutTelemetryRecords
            Resource: '*'
  EvidenceRuleFunction:
    Type: AWS::Lambda::Function
    DependsOn:
    - EvidenceRuleLogGroup
    Properties:
      Handler: evidence_existence_rule.lambda_handler
      Runtime: python3.12
      Timeout: 30
      ReservedConcurrentExecutions: 10
      TracingConfig:
        Mode: Active
      LoggingConfig:
        LogFormat: JSON
        ApplicationLogLevel: INFO
        SystemLogLevel: INFO
        LogGroup:
          Fn::Sub: /aws/lambda/${EvidenceRuleFunction}
      Role:
        Fn::GetAtt:
        - EvidenceRuleRole
        - Arn
      Code:
        S3Bucket:
          Ref: LambdaCodeS3Bucket
        S3Key:
          Ref: LambdaCodeS3Key
  EvidenceRuleInvokePermission:
    Type: AWS::Lambda::Permission
    Properties:
      Action: lambda:InvokeFunction
      FunctionName:
        Fn::GetAtt:
        - EvidenceRuleFunction
        - Arn
      Principal: config.amazonaws.com
  SdrCedRatTrainingRecordsRule:
    Type: AWS::Config::ConfigRule
    DependsOn:
    - EvidenceRuleInvokePermission
    Properties:
      ConfigRuleName: sdr-ced-rat-training-records
      Description: 'KSI-CED-RAT: training-records object covering each required audience.
        Presence+freshness telemetry, not a determination.'
      InputParameters:
        evidence_bucket:
          Ref: EvidenceBucket
        evidence_key: sdr-ced-rat-training-records/evidence
        max_age_days: 365
      Source:
        Owner: CUSTOM_LAMBDA
        SourceIdentifier:
          Fn::GetAtt:
          - EvidenceRuleFunction
          - Arn
        SourceDetails:
        - EventSource: aws.config
          MessageType: ScheduledNotification
          MaximumExecutionFrequency: TwentyFour_Hours
  SdrCmtRvpChangeProcedureReviewRule:
    Type: AWS::Config::ConfigRule
    DependsOn:
    - EvidenceRuleInvokePermission
    Properties:
      ConfigRuleName: sdr-cmt-rvp-change-procedure-review
      Description: 'KSI-CMT-RVP: change-management procedure document, reviewed. Presence+freshness
        telemetry, not a determination.'
      InputParameters:
        evidence_bucket:
          Ref: EvidenceBucket
        evidence_key: sdr-cmt-rvp-change-procedure-review/evidence
        max_age_days: 365
      Source:
        Owner: CUSTOM_LAMBDA
        SourceIdentifier:
          Fn::GetAtt:
          - EvidenceRuleFunction
          - Arn
        SourceDetails:
        - EventSource: aws.config
          MessageType: ScheduledNotification
          MaximumExecutionFrequency: TwentyFour_Hours
  SdrInrAarAfterActionReportsRule:
    Type: AWS::Config::ConfigRule
    DependsOn:
    - EvidenceRuleInvokePermission
    Properties:
      ConfigRuleName: sdr-inr-aar-after-action-reports
      Description: 'KSI-INR-AAR: after-action report for each closed incident. Presence+freshness
        telemetry, not a determination.'
      InputParameters:
        evidence_bucket:
          Ref: EvidenceBucket
        evidence_key: sdr-inr-aar-after-action-reports/evidence
        max_age_days: 90
      Source:
        Owner: CUSTOM_LAMBDA
        SourceIdentifier:
          Fn::GetAtt:
          - EvidenceRuleFunction
          - Arn
        SourceDetails:
        - EventSource: aws.config
          MessageType: ScheduledNotification
          MaximumExecutionFrequency: TwentyFour_Hours
  SdrInrRirIrProcedureReviewRule:
    Type: AWS::Config::ConfigRule
    DependsOn:
    - EvidenceRuleInvokePermission
    Properties:
      ConfigRuleName: sdr-inr-rir-ir-procedure-review
      Description: 'KSI-INR-RIR: incident response procedure and contact list, reviewed.
        Presence+freshness telemetry, not a determination.'
      InputParameters:
        evidence_bucket:
          Ref: EvidenceBucket
        evidence_key: sdr-inr-rir-ir-procedure-review/evidence
        max_age_days: 365
      Source:
        Owner: CUSTOM_LAMBDA
        SourceIdentifier:
          Fn::GetAtt:
          - EvidenceRuleFunction
          - Arn
        SourceDetails:
        - EventSource: aws.config
          MessageType: ScheduledNotification
          MaximumExecutionFrequency: TwentyFour_Hours
  SdrInrRpiIncidentHistoryRetentionRule:
    Type: AWS::Config::ConfigRule
    DependsOn:
    - EvidenceRuleInvokePermission
    Properties:
      ConfigRuleName: sdr-inr-rpi-incident-history-retention
      Description: 'KSI-INR-RPI: incident history data store retention configuration
        record. Presence+freshness telemetry, not a determination.'
      InputParameters:
        evidence_bucket:
          Ref: EvidenceBucket
        evidence_key: sdr-inr-rpi-incident-history-retention/evidence
        max_age_days: 365
      Source:
        Owner: CUSTOM_LAMBDA
        SourceIdentifier:
          Fn::GetAtt:
          - EvidenceRuleFunction
          - Arn
        SourceDetails:
        - EventSource: aws.config
          MessageType: ScheduledNotification
          MaximumExecutionFrequency: TwentyFour_Hours
  SdrMlaRvlLogReviewConfigRule:
    Type: AWS::Config::ConfigRule
    DependsOn:
    - EvidenceRuleInvokePermission
    Properties:
      ConfigRuleName: sdr-mla-rvl-log-review-config
      Description: 'KSI-MLA-RVL: scheduled log-review queries and alerting configuration
        record. Presence+freshness telemetry, not a determination.'
      InputParameters:
        evidence_bucket:
          Ref: EvidenceBucket
        evidence_key: sdr-mla-rvl-log-review-config/evidence
        max_age_days: 90
      Source:
        Owner: CUSTOM_LAMBDA
        SourceIdentifier:
          Fn::GetAtt:
          - EvidenceRuleFunction
          - Arn
        SourceDetails:
        - EventSource: aws.config
          MessageType: ScheduledNotification
          MaximumExecutionFrequency: TwentyFour_Hours
  SdrPiyResExecutiveReviewRule:
    Type: AWS::Config::ConfigRule
    DependsOn:
    - EvidenceRuleInvokePermission
    Properties:
      ConfigRuleName: sdr-piy-res-executive-review
      Description: 'KSI-PIY-RES: executive support review artifact. Presence+freshness
        telemetry, not a determination.'
      InputParameters:
        evidence_bucket:
          Ref: EvidenceBucket
        evidence_key: sdr-piy-res-executive-review/evidence
        max_age_days: 365
      Source:
        Owner: CUSTOM_LAMBDA
        SourceIdentifier:
          Fn::GetAtt:
          - EvidenceRuleFunction
          - Arn
        SourceDetails:
        - EventSource: aws.config
          MessageType: ScheduledNotification
          MaximumExecutionFrequency: TwentyFour_Hours
  SdrPiyRisInvestmentReviewRule:
    Type: AWS::Config::ConfigRule
    DependsOn:
    - EvidenceRuleInvokePermission
    Properties:
      ConfigRuleName: sdr-piy-ris-investment-review
      Description: 'KSI-PIY-RIS: security investment review artifact. Presence+freshness
        telemetry, not a determination.'
      InputParameters:
        evidence_bucket:
          Ref: EvidenceBucket
        evidence_key: sdr-piy-ris-investment-review/evidence
        max_age_days: 365
      Source:
        Owner: CUSTOM_LAMBDA
        SourceIdentifier:
          Fn::GetAtt:
          - EvidenceRuleFunction
          - Arn
        SourceDetails:
        - EventSource: aws.config
          MessageType: ScheduledNotification
          MaximumExecutionFrequency: TwentyFour_Hours
  SdrPiyRvdVulnDisclosureRule:
    Type: AWS::Config::ConfigRule
    DependsOn:
    - EvidenceRuleInvokePermission
    Properties:
      ConfigRuleName: sdr-piy-rvd-vuln-disclosure
      Description: 'KSI-PIY-RVD: vulnerability disclosure program document and intake
        config. Presence+freshness telemetry, not a determination.'
      InputParameters:
        evidence_bucket:
          Ref: EvidenceBucket
        evidence_key: sdr-piy-rvd-vuln-disclosure/evidence
        max_age_days: 365
      Source:
        Owner: CUSTOM_LAMBDA
        SourceIdentifier:
          Fn::GetAtt:
          - EvidenceRuleFunction
          - Arn
        SourceDetails:
        - EventSource: aws.config
          MessageType: ScheduledNotification
          MaximumExecutionFrequency: TwentyFour_Hours
  SdrRplArpRecoveryPlanReviewRule:
    Type: AWS::Config::ConfigRule
    DependsOn:
    - EvidenceRuleInvokePermission
    Properties:
      ConfigRuleName: sdr-rpl-arp-recovery-plan-review
      Description: 'KSI-RPL-ARP: recovery plan artifact, reviewed. Presence+freshness
        telemetry, not a determination.'
      InputParameters:
        evidence_bucket:
          Ref: EvidenceBucket
        evidence_key: sdr-rpl-arp-recovery-plan-review/evidence
        max_age_days: 365
      Source:
        Owner: CUSTOM_LAMBDA
        SourceIdentifier:
          Fn::GetAtt:
          - EvidenceRuleFunction
          - Arn
        SourceDetails:
        - EventSource: aws.config
          MessageType: ScheduledNotification
          MaximumExecutionFrequency: TwentyFour_Hours
  SdrRplRroRtoRpoReviewRule:
    Type: AWS::Config::ConfigRule
    DependsOn:
    - EvidenceRuleInvokePermission
    Properties:
      ConfigRuleName: sdr-rpl-rro-rto-rpo-review
      Description: 'KSI-RPL-RRO: documented RTO/RPO artifact, reviewed. Presence+freshness
        telemetry, not a determination.'
      InputParameters:
        evidence_bucket:
          Ref: EvidenceBucket
        evidence_key: sdr-rpl-rro-rto-rpo-review/evidence
        max_age_days: 365
      Source:
        Owner: CUSTOM_LAMBDA
        SourceIdentifier:
          Fn::GetAtt:
          - EvidenceRuleFunction
          - Arn
        SourceDetails:
        - EventSource: aws.config
          MessageType: ScheduledNotification
          MaximumExecutionFrequency: TwentyFour_Hours
