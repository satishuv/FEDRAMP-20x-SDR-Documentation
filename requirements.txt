# Runtime dependencies for the FedRAMP 20x Continuous Certification Package Framework.
# Pinned for reproducible builds. The framework itself is pure Python over the
# pinned dataset; these are only for schema validation and the Word export.
jsonschema==4.26.0
referencing==0.37.0
python-docx==1.2.0
# Offline ECDSA verification of evidence signatures against a pinned trusted
# signer (validation/scripts/validate_evidence.py). Well-known, actively
# maintained; used only to verify, never to sign.
cryptography==46.0.3
